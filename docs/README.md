# Documentation Index

Developer and architecture documentation for quick-local-db lives in this `docs/` directory.

Key pages:

- `workflow.md` — describes the WAL engine, record format, index, compaction, and concurrency considerations.

Other resources:

- `playground/examples/` contains runnable examples.
- `test/` contains the test suite used to validate features.

If you are publishing to npm, keep `README.md` concise — use this docs folder for design and developer-level notes.
